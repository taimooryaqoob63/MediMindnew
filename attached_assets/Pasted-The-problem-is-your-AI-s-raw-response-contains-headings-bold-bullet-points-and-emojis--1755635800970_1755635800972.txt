The problem is your AI’s raw response contains headings (###), bold (**), bullet points, and emojis — but your renderer isn’t formatting it properly. Instead, you’re seeing stuff like:
### Key Characteristics:
Autoimmune ConditionAutoimmune Condition: ...
That means:
1.	Markdown is not being parsed/rendered fully (so ### is printed instead of a styled heading).
2.	Your formatAIResponse cleanup might be duplicating text (like Autoimmune ConditionAutoimmune Condition).
3.	Without lightweight CSS for Markdown, everything looks crowded.
________________________________________
✅ Fix in Two Steps
1. Use a Proper Markdown Renderer
You already have a MarkdownRenderer in your code. Make sure it uses something like react-markdown:
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm"; // supports lists, tables, checkboxes

export const MarkdownRenderer = ({ content, className }: { content: string, className?: string }) => {
  return (
    <div className={`chat-markdown ${className || ""}`}>
      <ReactMarkdown remarkPlugins={[remarkGfm]}>
        {content}
      </ReactMarkdown>
    </div>
  );
};
This ensures:
•	### Headings → real headings
•	- lists → bulleted lists
•	**bold** → bold text
•	1. numbered → ordered list
•	✅, ⚠️, 🚨 emojis → stay as emojis
________________________________________
2. Lightweight Chat Markdown Styles
Create a file chat-markdown.css (or Tailwind plugin) to keep it clean like ChatGPT:
.chat-markdown {
  font-size: 0.9rem;
  line-height: 1.6;
}

.chat-markdown h3 {
  font-size: 1rem;
  font-weight: 600;
  margin: 0.75rem 0 0.5rem;
}

.chat-markdown p {
  margin: 0.5rem 0;
}

.chat-markdown ul,
.chat-markdown ol {
  margin: 0.5rem 0 0.5rem 1.25rem;
}

.chat-markdown li {
  margin: 0.25rem 0;
}

.chat-markdown strong {
  font-weight: 600;
}

.chat-markdown code {
  background: #f3f4f6;
  padding: 2px 4px;
  border-radius: 4px;
  font-size: 0.85em;
}
________________________________________
🔥 Result
Your same AI answer:
### Key Characteristics:
- Autoimmune Condition: The body's immune system...
- Onset: Typically develops in children...
- Insulin Dependence: Individuals require...
👉 will display as a proper heading with a clean bulleted list 
________________________________________

Perfect 👍 — the duplication (Autoimmune ConditionAutoimmune Condition, OnsetOnset) is coming from your formatAIResponse function.
Right now, it’s probably trying to “highlight” keywords or regex-replace terms, but it ends up echoing them twice.
________________________________________
✅ Fixed formatAIResponse
Here’s a clean version that:
•	Removes accidental duplications,
•	Preserves Markdown formatting,
•	Makes headings/lists readable,
•	Doesn’t over-style (leaves it to your chat-markdown.css).
export function formatAIResponse(raw: string) {
  if (!raw) return { content: "" };

  let text = raw;

  // 🔹 Fix common duplication bug like "OnsetOnset:"
  text = text.replace(/(\b[A-Z][a-zA-Z ]+)\1/g, "$1");

  // 🔹 Normalize spacing around headings (###)
  text = text.replace(/#+\s*/g, (m) => `${m.trim()} `);

  // 🔹 Ensure bullet points start correctly
  text = text.replace(/-\s*/g, "- ");

  // 🔹 Fix bold markers (sometimes duplicated or missing space)
  text = text.replace(/\*\*(\s*)/g, "**");

  // 🔹 Trim extra spaces & newlines
  text = text.replace(/\n{3,}/g, "\n\n").trim();

  return { content: text };
}
________________________________________
🛠 What This Fixes
•	OnsetOnset: → Onset: ✅
•	Autoimmune ConditionAutoimmune Condition: → Autoimmune Condition: ✅
•	Headings (### Symptoms:) render cleanly.
•	Lists stay properly formatted (- Item).
•	No extra colors, no duplication.
________________________________________
🔥 Example Before & After
Before (your bug):
### Key Characteristics:
Autoimmune ConditionAutoimmune Condition: ...
- OnsetOnset: Typically develops...
After (with fix):
### Key Characteristics:
- Autoimmune Condition: The body's immune system mistakenly attacks...
- Onset: Typically develops in children...


